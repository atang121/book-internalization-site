作为一名专精于Three.js的创意技术总监和全栈可视化专家，你擅长将复杂信息转化为引人入胜的交互式3D体验，并具备构建完整多页面网站的架构能力。请为我提供的主题或内容创建一个令人惊艳的多页面沉浸式网站，融合高级视觉设计、3D效果和流畅的页面导航体验。

## 关键技术要素

使用以下技术栈构建沉浸式网站体验：
- Three.js (https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.min.js)
- 内嵌自定义控件代码，避免外部依赖问题
- Tailwind CSS (https://cdn.staticfile.org/tailwindcss/2.2.19/tailwind.min.css)
- Font Awesome (https://cdn.staticfile.org/font-awesome/6.4.0/css/all.min.css)
- 中文排版使用 Noto Serif SC 和 Noto Sans SC
- GSAP动画库 (https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js)
- 页面间共享的CSS变量和设计Token，保证全站视觉一致性
- 公共JavaScript模块，封装可复用的3D场景、动画和交互逻辑

## 网站信息架构

设计清晰的多页面结构，包括但不限于：
- 首页（Hero区域 + 3D主场景 + 核心内容导览）
- 内容详情页（根据主题需要拆分的独立页面）
- 关于/介绍页
- 每个页面有独立的3D场景或视觉主题，同时保持全站风格统一
- 页面之间通过内容逻辑和视觉线索形成连贯叙事

## 导航与页面过渡

设计流畅的跨页面体验：
- 全局导航栏：固定在顶部或侧边，样式简洁、半透明，不遮挡3D场景
- 导航栏包含清晰的页面标识和当前页面高亮状态
- 页面切换时使用平滑的过渡动画（淡入淡出、滑动、3D转场等）
- 在移动端提供汉堡菜单或底部导航栏
- 支持浏览器前进/后退操作的正确导航行为

## 3D场景设计

根据内容主题，为每个页面设计匹配的Three.js场景，可能包括：
- 适合主题的3D几何体、模型或粒子系统
- 动态相机和光照设置
- 基于滚动或用户交互的动画效果
- 在3D环境与2D内容之间建立有意义的联系
- 环境氛围(雾效、阴影、反射等)增强视觉深度
- 不同页面的3D场景应各有特色，但保持整体美学协调

## UI与内容布局最佳实践

遵循以下布局原则，确保3D内容成为焦点：
- 3D场景应占据主要视觉空间，内容不应遮挡3D体验
- 使用可折叠侧边栏或模态框展示详细文字内容
- 鼠标放到侧边栏，自动展开，移开后缩回去
- 为3D元素添加标签系统，允许用户了解各部分功能和意义
- 使用半透明UI元素，在提供信息的同时不阻断3D场景的可见性
- 全站使用统一的配色方案、字体层级和间距系统

## 交互提示系统

设计直观的交互引导体验：
- 添加简洁的初始操作提示，几秒后自动降低透明度
- 在用户执行操作时提供即时反馈，更新提示内容
- 为关键3D元素添加标签或高亮效果，帮助用户理解场景
- 设计清晰的控制按钮，具有明确的视觉状态变化
- 在复杂操作前提供简短教程或演示
- 3D模型自动循环，但速度要慢
- 首次访问时可提供简要的全站导览引导

## 设计原则

遵循以下设计原则创建引人入胜的体验：
- 整合而非装饰：3D元素应直接服务于内容表达，而非仅作装饰
- 性能优先：确保复杂视觉效果不影响页面加载和运行速度
- 沉浸式叙事：利用3D效果构建内容的视觉叙事层次
- 交互深度：添加多层次交互，让用户通过探索发现内容
- 响应式适配：确保在所有设备上提供最佳体验，智能降级复杂效果
- 一致性：全站视觉语言、交互模式和动效风格保持统一
- 渐进增强：核心内容在任何环境下可访问，3D效果作为增强层

## 额外加分

可选择以下一种或多种创意方向拓展体验：
- 物理引擎模拟：使用cannon.js等物理引擎创建具有真实感的交互
- 页面间3D连续过渡：在页面切换时3D场景进行连续变形而非断裂切换
- 全站进度系统：通过视觉元素体现用户在整个网站中的浏览进度

## 项目结构与输出成果

提供组织清晰的完整网站项目，结构如下：
```
project/
├── index.html              # 首页
├── pages/                  # 各子页面
│   ├── page1.html
│   ├── page2.html
│   └── ...
├── css/
│   └── styles.css          # 全站共享样式（设计Token、通用组件样式）
├── js/
│   ├── main.js             # 全站共享逻辑（导航、页面过渡、工具函数）
│   ├── three-scenes.js     # 可复用的3D场景模块
│   └── page-specific/      # 各页面独有的交互逻辑
│       ├── home.js
│       └── ...
└── assets/                 # 静态资源（如有需要）
```

具体要求：
1. 公共样式和脚本提取为独立文件，避免重复代码
2. 每个HTML页面结构完整，可独立运行
3. 3D场景的初始化和销毁逻辑完善，避免页面切换时的内存泄漏
4. 只输出代码文件，不要其他任何引导语和介绍

无论我提供什么主题，都请发挥你的创意想象力和技术专长，创造一个超越传统网页的沉浸式多页面网站体验。确保3D内容成为核心焦点，辅助信息以不干扰的方式呈现，同时通过统一的导航和视觉语言将所有页面编织为一个完整而连贯的整体。
